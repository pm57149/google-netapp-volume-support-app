import React from 'react';
import Layout from '../components/Layout';
import PoolsTree from '../components/PoolsTree';

const PoolsPage = () => {
  return (
    <Layout>
      <PoolsTree />
    </Layout>
  );
};

export default PoolsPage;
